spring:
  datasource:
    url: jdbc:tc:postgresql:16:///catalogue?TC_DAEMON=true
    username: cotalogue
    password: cotalogue
  boot:
    admin:
      client:
        enabled: false
  cloud:
    config:
      enabled: false
  eureka:
    client:
      enabled: false