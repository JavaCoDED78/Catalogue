server:
  port: 8083
spring:
  application:
    name: catalogue-customer-app
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://config:config-password@localhost:8888