[source,bash]
----
$ curl 'http://localhost:8080/feedback-api/product-reviews' -i -X POST \
    -H 'Content-Type: application/json' \
    -d '{
  "productId" : 1,
  "rating" : 5,
  "review" : "На пяторочку!"
}'
----